---
import Section from "../components/Section.astro";

import { education } from "../data/resume.json" assert { type: "json" };
const { carrers, courses } = education;
---

<Section tittle="Educación">
  <h3>CARRERA PROFESIONAL</h3>
  {
    carrers.map(({ institution, url, area, studyType, startDate, endDate }) => {
      return (
        <div class="contenedor">
          <div class="infoInstitucion">
            <h4>{institution}</h4>
            <h5 class="tituloEducativo">{area}</h5>
            <h6 class="tituloEducativo">Carrera {studyType}</h6>
          </div>
          <div class="infoFechaUrl" style="text-align: right">
            <h6>
              {startDate} a {endDate}
            </h6>
            {/*             <a href=`${url}` target="_blank" rel= "noopener noreferrer">
              {url}
            </a> */}
          </div>
        </div>
      );
    })
  }
  <h3>CURSOS Y DIPLOMADOS</h3>
  {
    courses.map(({ institution, url, area, studyType, startDate, endDate }) => {
      return (
        <div class="contenedor">
          <div class="infoInstitucion">
            <h4>{institution}</h4>
            <h5 class="tituloEducativo">{area}</h5>
            <h6 class="tituloEducativo">{studyType}</h6>
          </div>
          <div class="infoFechaUrl" style="text-align: right">
            <h6 style="margin-top: 1.1rem">
              {startDate} a {endDate}
            </h6>
          </div>
        </div>
      );
    })
  }
</Section>

<style>
  .contenedor {
    display: flex;
    justify-content: space-between;
    margin-bottom: 1rem;
    gap: 3vw;
  }
  h5 {
    margin-bottom: 10px;
  }

  h5 {
    padding: 0;
  }

  .contenedor h4 {
    font-weight: 555;
    color: #222;
    margin-top: 1rem;
  }
  .contenedor h5 {
    font-weight: 550;
    color: #444;
    margin: 0.4rem 0 0.5rem 0;
    font-family: Menlo, Monaco, "Lucida Console", "Courier New", monospace;
  }
  .tituloEducativo {
    opacity: 0.8;
    margin: 0;
  }

  .infoFechaUrl {
    overflow-wrap: break-word;
    opacity: 0.9;
    max-width: 45%;
  }
  .infoInstitucion {
    overflow-wrap: break-word;
    max-width: 50%;
  }
  p {
    font-weight: 500;
    margin: 0 0 1rem 0;
  }
  h3 {
    font-weight: 700;
    color: #111;
    margin-top: 2rem;
  }
  h6 {
    font-weight: 555;
    color: #000;
  }

  @media screen and (max-width: 600px) {
    .contenedor {
      flex-direction: column;
      text-align: justify;
    }
    .contenedor h5 {
      margin: 0 0 2px 0;
    }
    .infoInstitucion {
      max-width: 100%;
    }
    .infoFechaUrl {
      margin-top: 0;
      margin-bottom: 25px;
      max-width: 100%;
    }
    .infoFechaUrl h5 {
      margin: 0;
      font-weight: 500;
    }
    h6 {
      margin: 0;
    }
  }
</style>
